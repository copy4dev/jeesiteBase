<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.debug.dao.DebugUserDetailDao">
    
	<sql id="debugUserDetailColumns">
		a.id AS "id",
		a.user_id AS "debugUser.id",
		a.height AS "height",
		a.birthday AS "birthday"
	</sql>
	
	<sql id="debugUserDetailJoins">
		LEFT JOIN debug_user b ON b.id = a.user_id
	</sql>
    
	<select id="get" resultType="DebugUserDetail">
		SELECT 
			<include refid="debugUserDetailColumns"/>
		FROM debug_user_detail a
		<include refid="debugUserDetailJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="DebugUserDetail">
		SELECT 
			<include refid="debugUserDetailColumns"/>
		FROM debug_user_detail a
		<include refid="debugUserDetailJoins"/>
		<where>
			
			<if test="debugUser != null and debugUser.id != null and debugUser.id != ''">
				AND a.user_id = #{debugUser.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="DebugUserDetail">
		SELECT 
			<include refid="debugUserDetailColumns"/>
		FROM debug_user_detail a
		<include refid="debugUserDetailJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO debug_user_detail(
			id,
			user_id,
			height,
			birthday
		) VALUES (
			#{id},
			#{debugUser.id},
			#{height},
			#{birthday}
		)
	</insert>
	
	<update id="update">
		UPDATE debug_user_detail SET 	
			user_id = #{debugUser.id},
			height = #{height},
			birthday = #{birthday}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM debug_user_detail
		<choose>
			<when test="id !=null and id != ''">
				WHERE id = #{id}
			</when>
			<otherwise>
				WHERE user_id = #{debugUser.id}
			</otherwise>
		</choose>
	</update>
	
</mapper>